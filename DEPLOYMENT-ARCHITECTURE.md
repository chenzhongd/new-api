# 🏗️ New API 部署架构详解

## 🎯 架构总览

### 方案 1：单服务器架构（推荐入门）

```
┌─────────────────────────────────────────────────────────────┐
│                      互联网用户                             │
└────────────────┬─────────────────────────────────────────────┘
                 │ HTTPS (443)
                 │
        ┌────────▼────────────────────────────┐
        │   Nginx (反向代理)                   │
        │   - SSL/TLS 终止                     │
        │   - 请求路由                        │
        │   - 静态文件服务                    │
        └────┬─────────────┬───────────────────┘
             │ HTTP (3000) │ HTTP (docs)
             │             │
        ┌────▼──────┐  ┌──▼──────────────┐
        │ New API    │  │ VitePress 文档  │
        │ (Go 应用)  │  │ (静态文件)      │
        │ :3000     │  │ /docs           │
        └────┬──────┘  └─────────────────┘
             │
        ┌────┴─────────────────────────┐
        │                              │
    ┌───▼────┐                   ┌────▼────┐
    │ MySQL  │                   │ Redis   │
    │ :3306  │                   │ :6379   │
    └────────┘                   └─────────┘
        │                             │
    ┌───▼─────────────────────────────▼──┐
    │   Docker 容器网络 (new-api-network) │
    └──────────────────────────────────────┘
        │
    ┌───▼──────────────────────────────┐
    │   主机文件系统                   │
    │   - /deployment/data/mysql/      │
    │   - /deployment/data/redis/      │
    │   - /deployment/data/new-api/    │
    └──────────────────────────────────┘
```

---

### 方案 2：分离部署架构（推荐生产）

```
┌────────────────────────────────────────────────────────────┐
│                    互联网用户流量                          │
└──────────┬──────────────────────────┬──────────────────────┘
           │                          │
     ┌─────▼──────┐            ┌─────▼────────────┐
     │ DNS 解析   │            │ CDN / 反向代理  │
     │ (example.  │            │ (可选)           │
     │  com)      │            │                  │
     └─────┬──────┘            └─────┬────────────┘
           │                         │
     ┌─────▼──────────────┐    ┌────▼─────────────────┐
     │ api.example.com    │    │ docs.example.com     │
     │ (API 服务器)       │    │ (文档服务器)         │
     │                    │    │                      │
     │ ┌────────────────┐ │    │ ┌──────────────────┐ │
     │ │ Nginx 反向代理 │ │    │ │ Nginx 静态服务  │ │
     │ │ SSL/TLS        │ │    │ │ SSL/TLS         │ │
     │ └────┬───────────┘ │    │ └──────────────────┘ │
     │      │             │    │                      │
     │ ┌────▼──────────┐  │    │ ┌──────────────────┐ │
     │ │ New API       │  │    │ │ VitePress 文档   │ │
     │ │ (Docker)      │  │    │ │ (静态 HTML/CSS/ │ │
     │ │ :3000        │  │    │ │  JS)             │ │
     │ │              │  │    │ └──────────────────┘ │
     │ │ Go 应用       │  │    │                      │
     │ │ - 用户管理    │  │    │ 托管选项:            │
     │ │ - API 处理    │  │    │ - 自建 Nginx        │
     │ │ - 业务逻辑    │  │    │ - Vercel (推荐)    │
     │ │              │  │    │ - Netlify           │
     │ └────┬─────────┘  │    │ - GitHub Pages      │
     │      │            │    └──────────────────────┘
     │ ┌────▼──────┐     │
     │ │ Redis     │     │
     │ │ (缓存)    │     │
     │ │ :6379    │     │
     │ └───────────┘     │
     │                    │
     └─────────┬──────────┘
               │
        ┌──────▼──────────┐
        │ 共享数据库      │
        │ (MySQL/Postgres)│
        │ db.example.com  │
        │ :3306          │
        │                │
        │ 可选托管:       │
        │ - AWS RDS       │
        │ - Google Cloud  │
        │  SQL            │
        │ - 阿里云 RDS   │
        │ - 自建数据库    │
        └─────────────────┘
```

---

### 方案 3：Kubernetes 高可用部署

```
┌──────────────────────────────────────────────────────────────┐
│                   互联网用户流量                             │
└────────────┬──────────────────────────────┬─────────────────┘
             │                              │
        ┌────▼──────────┐            ┌─────▼────────┐
        │ 负载均衡器    │            │ 负载均衡器   │
        │ (AWS ELB/    │            │ (Nginx       │
        │  Google LB)   │            │  Ingress)    │
        └────┬──────────┘            └─────┬────────┘
             │                             │
   ┌─────────▼──────────────────────────────▼───────┐
   │        Kubernetes 集群 (K8s)                    │
   │                                                 │
   │  ┌────────────────────────────────────────────┐ │
   │  │  Namespace: new-api                        │ │
   │  │                                            │ │
   │  │  ┌─────────┐  ┌─────────┐  ┌─────────┐  │ │
   │  │  │ Pod     │  │ Pod     │  │ Pod     │  │ │
   │  │  │ (API)   │  │ (API)   │  │ (API)   │  │ │
   │  │  │ 副本 1  │  │ 副本 2  │  │ 副本 3  │  │ │
   │  │  └──┬──────┘  └──┬──────┘  └──┬──────┘  │ │
   │  │     │            │            │         │ │
   │  │  ┌──▼────────────▼────────────▼──┐    │ │
   │  │  │ Service: new-api (负载均衡)   │    │ │
   │  │  │ ClusterIP: 10.0.0.100         │    │ │
   │  │  └──────────────────────────────┘    │ │
   │  │                                       │ │
   │  │  ┌─────────────────────────────────┐ │ │
   │  │  │ StatefulSet: MySQL              │ │ │
   │  │  │ - 持久化存储 (PVC)              │ │ │
   │  │  │ - 副本数: 1 (主)               │ │ │
   │  │  └─────────────────────────────────┘ │ │
   │  │                                       │ │
   │  │  ┌─────────────────────────────────┐ │ │
   │  │  │ StatefulSet: Redis              │ │ │
   │  │  │ - 持久化存储 (PVC)              │ │ │
   │  │  │ - 副本数: 3 (Sentinel)         │ │ │
   │  │  └─────────────────────────────────┘ │ │
   │  │                                       │ │
   │  │  ┌─────────────────────────────────┐ │ │
   │  │  │ ConfigMap & Secret              │ │ │
   │  │  │ - 环境配置                      │ │ │
   │  │  │ - 数据库密码                    │ │ │
   │  │  └─────────────────────────────────┘ │ │
   │  └────────────────────────────────────────┘ │
   │                                              │
   │  ┌─────────────────────────────────────────┐ │
   │  │ Monitoring & Logging                    │ │
   │  │ - Prometheus (监控)                     │ │
   │  │ - Grafana (可视化)                      │ │
   │  │ - ELK Stack (日志)                      │ │
   │  │ - Jaeger (追踪)                         │ │
   │  └─────────────────────────────────────────┘ │
   │                                              │
   │  ┌─────────────────────────────────────────┐ │
   │  │ CI/CD Pipeline                          │ │
   │  │ - GitHub Actions                        │ │
   │  │ - ArgoCD                                │ │
   │  │ - Helm Charts                           │ │
   │  └─────────────────────────────────────────┘ │
   └────────────────────────────────────────────────┘
        │
        │ 块存储 (Block Storage)
        │ └─ AWS EBS / Google Persistent Disk
        │
        │ 对象存储 (Object Storage)
        │ └─ AWS S3 / Google Cloud Storage
```

---

## 📊 通信流程图

### API 请求流程

```
┌─────────────┐
│ 客户端      │
│ (浏览器)    │
└──────┬──────┘
       │ 1. 发送 HTTPS 请求
       │    GET /api/user/self
       │    Authorization: Bearer token
       │
       ▼
┌──────────────────────┐
│ Nginx 反向代理        │
│ - SSL 解密           │
│ - 请求验证           │
│ - 速率限制           │
└──────┬───────────────┘
       │ 2. 转发 HTTP 请求
       │    http://new-api:3000/api/user/self
       │
       ▼
┌────────────────────────┐
│ New API (Go 应用)      │
│ ┌─────────────────────┤
│ │ 路由处理            │
│ ├─────────────────────┤
│ │ 中间件验证          │
│ │ - JWT 令牌          │
│ │ - 权限检查          │
│ ├─────────────────────┤
│ │ 业务逻辑处理        │
│ │ - 查询用户信息      │
│ │ - 检查 Redis 缓存   │
│ │ - 查询 MySQL 数据库 │
│ │ - 生成响应          │
│ └─────────────────────┘
└──────┬───────────────────┘
       │ 3. 返回 JSON 响应
       │    {
       │      "id": 123,
       │      "email": "user@example.com",
       │      "balance": 100.0
       │    }
       │
       ▼
┌──────────────────────┐
│ Nginx 反向代理        │
│ - 压缩响应 (gzip)    │
│ - 添加 CORS 头       │
│ - 缓存处理           │
└──────┬───────────────┘
       │ 4. 返回 HTTPS 响应
       │
       ▼
┌──────────────────┐
│ 客户端浏览器      │
│ - 显示数据        │
└───────────────────┘
```

### 数据库操作流程

```
New API
  │
  │ 1. 检查 Redis 缓存
  │
  ▼
┌──────────────────┐     MISS
│ Redis (缓存)     │─────────→ (继续)
│ :6379           │
└──────────────────┘
         ▲
         │ HIT
         │ (返回缓存数据)
         │

2. 查询 MySQL 数据库
         │
         ▼
    ┌────────────────────┐
    │ MySQL (主数据库)   │
    │ :3306             │
    │                    │
    │ 执行 SQL:          │
    │ SELECT * FROM      │
    │   users WHERE      │
    │   id = ?           │
    └────────┬───────────┘
             │
             ▼

3. 处理结果
         │
         ├─ 缓存到 Redis
         │
         └─ 返回给应用
```

---

## 🔄 部署流程图

### Docker 本地构建和部署

```
源代码
  │
  ├─ backend (Go)
  │   │
  │   ├─ main.go
  │   ├─ controller/
  │   ├─ service/
  │   └─ model/
  │
  └─ frontend (React)
      │
      ├─ web/src/
      ├─ package.json
      └─ Dockerfile

         │
         ▼

1. 构建前端 (Bun)
         │
    bun run build
         │
         ▼
    web/dist/ (静态文件)


2. 构建 Go 应用 (Dockerfile)
         │
    docker build -t new-api:latest .
         │
         ▼
    Docker 镜像


3. 启动容器 (docker-compose)
         │
    docker-compose up -d
         │
         ├─ MySQL 容器
         ├─ Redis 容器
         ├─ New API 容器 (包含前端)
         └─ Nginx 容器


4. 验证服务
         │
         ├─ curl localhost:3000
         ├─ curl localhost:3306
         └─ curl localhost:6379
```

---

## 📝 网络配置详解

### Docker 网络隔离

```
Host 主机
│
├─ 物理网卡 (eth0)
│   │
│   └─ IP: 192.168.1.100
│
└─ Docker 网桥 (docker0)
    │
    └─ new-api-network (用户定义网络)
        │
        ├─ mysql (172.18.0.2:3306)
        │   │
        │   └─ 数据卷: /var/lib/mysql
        │
        ├─ redis (172.18.0.3:6379)
        │   │
        │   └─ 数据卷: /data
        │
        ├─ new-api (172.18.0.4:3000)
        │   │
        │   └─ 数据卷: /data
        │
        └─ nginx (172.18.0.5)
            │
            └─ 端口映射:
               - 0.0.0.0:80 -> :80
               - 0.0.0.0:443 -> :443

DNS 解析 (Docker 内部):
  mysql -> 172.18.0.2
  redis -> 172.18.0.3
  new-api -> 172.18.0.4
  nginx -> 172.18.0.5
```

### 防火墙和端口映射

```
互联网
  │
  └─ 端口 80 (HTTP)
     └─ nginx 容器:80
        └─ 重定向 -> 443

互联网
  │
  └─ 端口 443 (HTTPS)
     └─ nginx 容器:443
        └─ 转发 -> new-api:3000 (内部)

主机
  │
  ├─ 端口 3306 (MySQL)
  │  └─ mysql 容器:3306
  │     └─ 仅容器内访问 (未暴露)
  │
  ├─ 端口 6379 (Redis)
  │  └─ redis 容器:6379
  │     └─ 仅容器内访问 (未暴露)
  │
  └─ 端口 3000 (API 调试)
     └─ new-api 容器:3000
        └─ 供开发使用 (可选暴露)
```

---

## 🔐 数据流加密

```
HTTPS 加密通道
│
└─ TLS 1.2/1.3
   │
   ├─ Client Hello
   ├─ Server Certificate (SSL 证书)
   ├─ Key Exchange
   └─ Encrypted Connection

连接建立后:
│
├─ 所有数据加密传输
├─ Nginx 在内部解密
├─ 转发解密数据给后端 (HTTP)
├─ 后端处理并返回数据
├─ Nginx 加密响应
└─ 返回给客户端
```

---

## 📦 依赖关系图

```
┌─────────────────────────────────────────┐
│           应用启动顺序                  │
└─────────────────────────────────────────┘

1. MySQL (数据库)
   │
   └─ 初始化数据库和表
        │
        └─ 数据库就绪


2. Redis (缓存)
   │
   └─ 启动缓存服务
        │
        └─ 缓存就绪


3. New API (应用)
   │
   ├─ 等待 MySQL 就绪 (healthcheck)
   ├─ 等待 Redis 就绪 (healthcheck)
   │
   ├─ 连接 MySQL
   ├─ 连接 Redis
   │
   ├─ 初始化路由
   ├─ 启动 HTTP 服务器
   │
   └─ 应用就绪


4. Nginx (反向代理)
   │
   ├─ 等待 New API 就绪
   │
   ├─ 加载 SSL 证书
   ├─ 启动 HTTPS 服务
   │
   └─ 反向代理就绪


5. VitePress 文档
   │
   ├─ 静态文件
   ├─ 由 Nginx 直接服务
   │
   └─ 文档就绪
```

---

## 📊 存储架构

```
┌─────────────────────────────────────────┐
│         持久化存储 (Volumes)            │
└─────────────────────────────────────────┘

主机文件系统 (/deployment/data/)
│
├─ mysql/
│  │
│  ├─ ibdata1           (共享表空间)
│  ├─ mysql.ibd         (MySQL 系统表)
│  ├─ new_api.ibd       (应用数据库)
│  │
│  └─ 数据库文件 (表数据, 索引)
│
├─ redis/
│  │
│  ├─ dump.rdb          (快照备份)
│  ├─ appendonly.aof    (追加日志)
│  │
│  └─ 缓存数据
│
└─ new-api/
   │
   ├─ logs/             (应用日志)
   │  └─ app.log
   │
   ├─ uploads/          (用户上传文件)
   ├─ cache/            (本地缓存)
   │
   └─ config/           (配置文件)
```

---

## 🔄 扩展路径

### 从单服务器到分离部署

```
阶段 1: 单服务器 (1 个实例)
┌────────────────────────┐
│ New API + MySQL + Redis│
│ 最大用户: 1000         │
└────────────────────────┘

              │
              │ 性能瓶颈出现
              │ (CPU, 内存不足)
              ▼

阶段 2: 分离部署 (2 个实例)
┌──────────────┐  ┌─────────────────────┐
│ New API x 2  │  │ MySQL + Redis (共享)│
│ 最大用户: 10K│  └─────────────────────┘
└──────────────┘

              │
              │ 数据库成为瓶颈
              │
              ▼

阶段 3: 数据库分离 (3 个实例)
┌──────────────┐  ┌─────────────┐  ┌────────────────┐
│ New API x 2  │  │ MySQL 主从  │  │ Redis 集群     │
│ 最大用户: 50K│  └─────────────┘  └────────────────┘
└──────────────┘

              │
              │ 需要自动扩展
              │
              ▼

阶段 4: Kubernetes (N 个副本)
┌────────────────────────────────────┐
│ New API Pod x N (自动扩展)         │
│ MySQL (托管 RDS)                   │
│ Redis (托管缓存)                   │
│ 最大用户: 100K+                    │
└────────────────────────────────────┘
```

---

**Created**: 2024-01-01  
**Version**: 1.0.0  
**Status**: Production Ready
